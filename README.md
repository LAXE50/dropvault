<!doctype html><html lang="tr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#090b10"><title>DropVault</title><style>
:root{--bg:#080a0f;--p:#10131a;--p2:#151922;--b:#292f3c;--t:#f5f7fb;--m:#8d96a7;--v:#7c3aed;--v2:#a78bfa;--r:#ef4444;--g:#22c55e}*{box-sizing:border-box}html,body{margin:0;min-height:100%;background:var(--bg);color:var(--t);font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif}button,input,textarea{font:inherit}button{cursor:pointer} .hide{display:none!important}
.auth{min-height:100vh;display:grid;place-items:center;padding:20px;background:radial-gradient(circle at 20% 0,#6d28d922,transparent 35%),var(--bg)}.box{width:min(440px,100%);background:#10131af5;border:1px solid var(--b);border-radius:22px;padding:26px;box-shadow:0 30px 90px #0008}.brand{text-align:center;font-weight:900;font-size:30px;letter-spacing:-1px;user-select:none}.brand i{font-style:normal;color:var(--v2)}.sub{text-align:center;color:var(--m);margin:7px 0 23px}.tabs{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:18px}.tabs button,.btn{border:1px solid var(--b);background:var(--p2);color:var(--t);border-radius:10px;padding:11px}.tabs .on,.primary{background:linear-gradient(135deg,#6d28d9,#8b5cf6);border-color:#8b5cf6;color:white}.field{margin:13px 0}.field label{display:block;color:#aeb6c5;font-size:13px;margin-bottom:7px}.field input,.field textarea,.search{width:100%;border:1px solid #303746;background:#090c12;color:white;border-radius:10px;padding:12px;outline:none}.field input:focus,.field textarea:focus,.search:focus{border-color:var(--v2)}
header{height:66px;border-bottom:1px solid var(--b);background:#0d1016ee;backdrop-filter:blur(15px);display:flex;align-items:center;justify-content:space-between;padding:0 max(14px,calc((100% - 1100px)/2));position:sticky;top:0;z-index:20}.headbrand{font-size:21px;font-weight:900;user-select:none}.headbrand i{font-style:normal;color:var(--v2)}.headright{display:flex;gap:8px;align-items:center}.user{font-size:13px;color:#c4cad5;border:1px solid var(--b);border-radius:99px;padding:7px 10px}.main{width:min(1100px,94%);margin:auto;padding:25px 0 60px}.hero{border:1px solid var(--b);border-radius:20px;padding:27px;background:linear-gradient(135deg,#19102b,#10131a 65%);margin-bottom:16px}.hero h1{margin:0 0 8px;font-size:32px}.hero p{margin:0;color:var(--m);line-height:1.55}.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:16px}.stat{background:var(--p);border:1px solid var(--b);border-radius:14px;padding:15px}.stat small{color:var(--m)}.stat b{display:block;margin-top:5px;font-size:21px}.upload{border:1.5px dashed #485163;background:#0e1219;border-radius:18px;padding:34px 18px;text-align:center;transition:.2s}.upload.drag{border-color:var(--v2);background:#171027}.upload .up{font-size:38px}.upload h2{margin:8px 0}.upload p{color:var(--m);margin:0 0 15px}.filepick{display:inline-block;padding:11px 17px;border-radius:10px;background:var(--v);font-weight:800}.filepick input{display:none}.selected{color:#c4b5fd;font-size:13px;margin-top:12px}.section{display:flex;justify-content:space-between;align-items:center;gap:12px;margin:28px 0 14px}.section h2{margin:0}.search{max-width:300px}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(275px,1fr));gap:14px}.card{background:linear-gradient(145deg,#12161e,#0e1117);border:1px solid var(--b);border-radius:16px;padding:16px}.icon{width:46px;height:46px;display:grid;place-items:center;background:#22173a;border-radius:12px;font-size:21px;margin-bottom:10px}.fname{font-weight:800;word-break:break-word}.meta{font-size:11px;color:#737d8e;margin-top:6px;line-height:1.5}.desc{color:#b8bfcc;font-size:13px;line-height:1.5;min-height:39px;margin-top:11px;white-space:pre-wrap;word-break:break-word}.actions{display:grid;gap:7px;margin-top:13px}.actions button{padding:10px;border:1px solid var(--b);border-radius:9px;background:#171b24;color:white}.actions .dl{background:#6d28d9;border-color:#6d28d9}.actions .del{color:#ff9aa7;border-color:#5d2932}.empty{grid-column:1/-1;text-align:center;color:var(--m);padding:50px;border:1px dashed var(--b);border-radius:15px}.admin{background:linear-gradient(135deg,#190d2d,#100d16);border:1px solid #653cb4;border-radius:18px;padding:18px;margin-bottom:18px}.admin h2{margin:0 0 15px;color:#d7c7ff}.adminstats{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.ast{background:#0c0b11;border:1px solid #38204f;border-radius:11px;padding:14px}.ast small{color:#9488a2}.ast b{display:block;font-size:21px;margin-top:5px}.adminlist{margin-top:15px;max-height:300px;overflow:auto}.row{display:flex;justify-content:space-between;gap:10px;align-items:center;padding:10px 0;border-bottom:1px solid #302337;font-size:13px}.row span{color:#a9a1b2}.toast{position:fixed;left:50%;bottom:22px;transform:translate(-50%,20px);opacity:0;background:#171b24;border:1px solid var(--b);border-radius:10px;padding:11px 15px;z-index:99;transition:.2s;pointer-events:none}.toast.show{opacity:1;transform:translate(-50%,0)}
@media(max-width:700px){header{padding:0 12px}.user{display:none}.main{width:94%;padding-top:18px}.hero{padding:23px}.hero h1{font-size:27px}.stats{grid-template-columns:1fr}.section{align-items:stretch;flex-direction:column}.search{max-width:none}.adminstats{grid-template-columns:1fr}.grid{grid-template-columns:1fr}}
</style></head><body>
<div id="app"><section class="auth"><div class="box"><div class="brand">Drop<i>Vault</i></div><div class="sub">Dosya paylaşım alanına hoş geldin.</div><div class="tabs"><button id="lt" class="on">Giriş</button><button id="rt">Kayıt</button></div><div id="authForm"></div></div></section></div><div id="toast" class="toast"></div>
<script>
'use strict';
const UKEY='dv_users_1',FKEY='dv_files_1',SKEY='dv_session_1';let me=null,admin=false,logoTaps=0,logoTimer=0;const mem={};function lsGet(k){try{return localStorage.getItem(k)}catch(e){return mem[k]??null}}function lsSet(k,v){try{localStorage.setItem(k,v)}catch(e){mem[k]=v}}function lsDel(k){try{localStorage.removeItem(k)}catch(e){delete mem[k]}}
const $=id=>document.getElementById(id);const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const uid=()=>Date.now().toString(36)+'_'+Math.random().toString(36).slice(2);const users=()=>{try{return JSON.parse(lsGet(UKEY)||'[]')}catch{return[]}};const files=()=>{try{return JSON.parse(lsGet(FKEY)||'[]')}catch{return[]}};const saveU=x=>lsSet(UKEY,JSON.stringify(x));const saveF=x=>lsSet(FKEY,JSON.stringify(x));const sz=n=>n<1024?n+' B':n<1048576?(n/1024).toFixed(1)+' KB':n<1073741824?(n/1048576).toFixed(1)+' MB':(n/1073741824).toFixed(1)+' GB';
function toast(x){let t=$('toast');t.textContent=x;t.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>t.classList.remove('show'),2300)}
function loginForm(){
 $('lt').classList.add('on');$('rt').classList.remove('on');$('authForm').innerHTML=`<div class="field"><label>E-posta</label><input id="le" type="email" autocomplete="email"></div><div class="field"><label>Kullanıcı adı</label><input id="lu" autocomplete="username"></div><div class="field"><label>Şifre</label><input id="lp" type="password" autocomplete="current-password"></div><button class="btn primary" id="loginBtn">Giriş yap</button>`;$('loginBtn').onclick=login}
function regForm(){
 $('rt').classList.add('on');$('lt').classList.remove('on');$('authForm').innerHTML=`<div class="field"><label>E-posta</label><input id="re" type="email"></div><div class="field"><label>Kullanıcı adı</label><input id="ru"></div><div class="field"><label>Şifre</label><input id="rp" type="password"></div><div class="field"><label>Şifre tekrar</label><input id="rp2" type="password"></div><button class="btn primary" id="regBtn">Hesap oluştur</button>`;$('regBtn').onclick=register}
$('lt').onclick=loginForm;$('rt').onclick=regForm;loginForm();
function register(){let email=$('re').value.trim(),u=$('ru').value.trim(),p=$('rp').value;if(!email||!u||!p)return toast('Tüm alanları doldur.');if(p!==$('rp2').value)return toast('Şifreler aynı değil.');if(u.length<3)return toast('Kullanıcı adı en az 3 karakter.');let a=users();if(a.some(x=>x.username.toLowerCase()===u.toLowerCase()))return toast('Kullanıcı adı zaten alınmış.');if(a.some(x=>x.email.toLowerCase()===email.toLowerCase()))return toast('Bu e-posta zaten kayıtlı.');a.push({id:uid(),username:u,email,password:p,created:Date.now()});saveU(a);toast('Hesap oluşturuldu.');setTimeout(loginForm,500)}
function login(){let e=$('le').value.trim(),u=$('lu').value.trim(),p=$('lp').value;if(u==='PİERROTXD'&&p==='K3yM4st3r!@#2026X'){admin=true;me={id:'ADMIN',username:'PİERROTXD',email:e||'admin@dropvault.local'};lsSet(SKEY,'__ADMIN__');dashboard();setTimeout(openAdmin,0);return}let a=users(),x=a.find(x=>x.username.toLowerCase()===u.toLowerCase()&&x.email.toLowerCase()===e.toLowerCase()&&x.password===p);if(!x)return toast('Giriş bilgileri hatalı.');me=x;lsSet(SKEY,x.username);dashboard()}
function logout(){me=null;admin=false;lsDel(SKEY);location.reload()}
function dashboard(){
 $('app').innerHTML=`<header><div class="headbrand" id="secretLogo">Drop<i>Vault</i></div><div class="headright"><span class="user">@${esc(me.username)}</span><button class="btn" id="logout">Çıkış</button></div></header><main class="main"><section class="hero"><h1>Dosyalarını paylaş.</h1><p>APK, IPA, EXE, ZIP, PDF, TXT ve diğer dosyalarını yükle. Kendi dosyalarını düzenle veya sil.</p></section><div id="adminSlot"></div><section class="stats"><div class="stat"><small>TOPLAM DOSYA</small><b id="total">0</b></div><div class="stat"><small>BENİM DOSYALARIM</small><b id="mine">0</b></div><div class="stat"><small>KULLANICI</small><b>@${esc(me.username)}</b></div></section><section class="upload" id="drop"><div class="up">⬆</div><h2>Dosya yükle</h2><p>Dosyanı seç veya buraya sürükle.</p><label class="filepick">Dosya seç<input id="fi" type="file"></label><div class="selected" id="sel"></div></section><div class="section"><h2>Paylaşılan dosyalar</h2><input class="search" id="search" placeholder="Dosya veya kullanıcı ara..."></div><div class="grid" id="list"></div></main>`;
 $('logout').onclick=logout;$('search').oninput=render;$('fi').onchange=e=>pick(e.target.files[0]);let d=$('drop');d.ondragover=e=>{e.preventDefault();d.classList.add('drag')};d.ondragleave=()=>d.classList.remove('drag');d.ondrop=e=>{e.preventDefault();d.classList.remove('drag');pick(e.dataTransfer.files[0])};$('secretLogo').onclick=secretTap;render();stats()}
function secretTap(){logoTaps++;clearTimeout(logoTimer);logoTimer=setTimeout(()=>logoTaps=0,1200);if(logoTaps>=5){logoTaps=0;adminLogin()}}
function adminLogin(){let u=prompt('Yönetici kullanıcı adı:');if(u===null)return;let p=prompt('Yönetici şifresi:');if(u==='PİERROTXD'&&p==='K3yM4st3r!@#2026X'){admin=true;openAdmin()}else toast('Yetkisiz giriş.')}
function openAdmin(){let us=users(),fs=files(),slot=$('adminSlot');slot.innerHTML=`<section class="admin"><h2>⚡ Yönetici Paneli</h2><div class="adminstats"><div class="ast"><small>KULLANICI</small><b>${us.length}</b></div><div class="ast"><small>DOSYA</small><b>${fs.length}</b></div><div class="ast"><small>TOPLAM BOYUT</small><b>${sz(fs.reduce((n,x)=>n+x.size,0))}</b></div></div><div class="adminlist">${fs.length?fs.map(f=>`<div class="row"><span>${esc(f.name)} · @${esc(f.owner)} · ${sz(f.size)}</span><button class="btn" onclick="adminDelete('${f.id}')">Sil</button></div>`).join(''):'<span class="muted">Dosya yok.</span>'}</div></section>`}
function adminDelete(id){if(!admin)return;let a=files();let f=a.find(x=>x.id===id);if(!f)return;if(confirm('Bu dosyayı yönetici olarak sil?')){saveF(a.filter(x=>x.id!==id));openAdmin();render();stats();toast('Dosya silindi.')}}
function pick(file){if(!file)return;if(file.size>3*1024*1024)return toast('Demo sürümünde dosya sınırı 3 MB.');let desc=prompt('Dosya açıklaması:','');if(desc===null)return;let r=new FileReader();r.onload=()=>{let a=files();a.push({id:uid(),name:file.name,size:file.size,type:file.type,data:r.result,description:desc,owner:me.username,created:Date.now()});try{saveF(a)}catch(e){return toast('Tarayıcı depolama sınırına ulaşıldı.')}$('sel').textContent=file.name+' · '+sz(file.size);render();stats();if(admin)openAdmin();toast('Dosya paylaşıldı.')};r.onerror=()=>toast('Dosya okunamadı.');r.readAsDataURL(file)}
function render(){let q=($('search')?.value||'').toLowerCase().trim(),a=files().filter(f=>f.name.toLowerCase().includes(q)||f.owner.toLowerCase().includes(q)).sort((a,b)=>b.created-a.created),l=$('list');if(!a.length){l.innerHTML='<div class="empty">Henüz dosya bulunamadı.</div>';return}l.innerHTML=a.map(f=>{let own=f.owner===me.username;return `<article class="card"><div class="icon">📄</div><div class="fname">${esc(f.name)}</div><div class="meta">${sz(f.size)} · @${esc(f.owner)}<br>${new Date(f.created).toLocaleString('tr-TR')}</div><div class="desc">${esc(f.description)||'Açıklama yok.'}</div><div class="actions"><button class="dl" onclick="download('${f.id}')">${own||admin?'İndir':''}</button>${own?`<button onclick="edit('${f.id}')">Açıklamayı düzenle</button><button class="del" onclick="delOwn('${f.id}')">Sil</button>`:''}</div></article>`}).join('')}
function stats(){if(!$('total'))return;let a=files();$('total').textContent=a.length;$('mine').textContent=a.filter(x=>x.owner===me.username).length}
function download(id){let f=files().find(x=>x.id===id);if(!f)return;if(!admin&&f.owner!==me.username)return toast('Başkasının dosyası indirilemez.');let a=document.createElement('a');a.href=f.data;a.download=f.name;document.body.appendChild(a);a.click();a.remove()}
function edit(id){let a=files(),f=a.find(x=>x.id===id);if(!f||f.owner!==me.username)return toast('Bu dosya sana ait değil.');let d=prompt('Yeni açıklama:',f.description||'');if(d===null)return;f.description=d;saveF(a);render();toast('Açıklama güncellendi.')}
function delOwn(id){let a=files(),f=a.find(x=>x.id===id);if(!f||f.owner!==me.username)return toast('Bu dosya sana ait değil.');if(!confirm('Dosya silinsin mi?'))return;saveF(a.filter(x=>x.id!==id));render();stats();toast('Dosya silindi.')}
(function restore(){try{let u=lsGet(SKEY);if(u==='__ADMIN__'){admin=true;me={id:'ADMIN',username:'PİERROTXD',email:'admin@dropvault.local'};dashboard();setTimeout(openAdmin,0);return}if(u){let x=users().find(x=>x.username===u);if(x){me=x;dashboard();return}}}catch(e){} })();
</script></body></html>
